import React from "react";
import {Pie} from 'react-chartjs-2';

export default function Chart({chartData}) {
    return (
        <div className="w-[50%]">
            {/* <Pie data={chartData}/> */}
        </div>
    );
}
